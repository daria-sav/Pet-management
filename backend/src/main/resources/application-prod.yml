spring:
  datasource:
    url: jdbc:postgresql://localhost:5436/pets
    username: postgres
    password: postgres
  jpa: # Java Persistence API
    hibernate:
      ddl-auto: validate
    properties:
      hibernate.format_sql: true
  liquibase:
    enabled: true
    change-log: classpath:/db/changelog/db.changelog-master.xml

server:
  port: 8080

app:
  jwt:
    secret: "thisIsMysecregtfrdesww233eggtffeeddgkjjhhtdhttebd54ndhdhfhhhshs8877465sbbdd"
    expirationMillis: 86400000 # 24h